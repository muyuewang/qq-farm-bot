syntax = "proto3";

package gamepb.paypb;

// 支付系统下发的余额摘要。钻石不属于普通背包物品，不能通过 ItemService.Bag 获取。
message RechargeInfo {
    int64 diamond = 1;
}

message RechargeInfoNotify {
    RechargeInfo recharge_info = 1;
    string scene = 2;
}
